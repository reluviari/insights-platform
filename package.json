{
  "name": "insights-platform",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "nx": "nx",
    "graph": "nx graph",
    "affected": "nx affected",
    "build:api": "nx run insights-api:build",
    "build:web": "nx run insights-web:build",
    "dev:api": "nx run insights-api:dev",
    "dev:web": "nx run insights-web:dev",
    "test:api": "nx run insights-api:test",
    "lint:api": "nx run insights-api:lint",
    "lint:web": "nx run insights-web:lint",
    "lint": "nx run-many -t lint --all",
    "test": "nx run-many -t test --all",
    "many:build": "nx run-many -t build --all",
    "seed:mongo:dev": "mongosh \"${MONGODB_URI:-mongodb://127.0.0.1:27017/qa-pbi}\" --file docker/mongo/seed-insights-keycloak-dev.js"
  },
  "devDependencies": {
    "nx": "20.6.4"
  },
  "workspaces": [
    "insights.api",
    "insights.web"
  ]
}
