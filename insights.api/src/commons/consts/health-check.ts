const available = "Verifies the availability of the";

export enum HealthCheckConstants {
  APPLICATION = `${available} application`,
  DATABASE = `${available} database`,
  POWER_BI_EMBED = `${available} power bi embed`,
}
