# Substitua por um fluxo da sua organização (Sonar, Snyk, CodeQL, etc.).
# O workflow reutilizável da foundation foi removido deste fork.

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  security-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lembrete
        run: |
          echo "Configure aqui o scanner de segurança corporativo (ou desative este job)."
